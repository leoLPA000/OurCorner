# 🔐 Flujo de Autenticación - Diagrama Visual

## 📊 Arquitectura del Sistema

```
┌─────────────────────────────────────────────────────────────────┐
│                        GITHUB PAGES                              │
│                    (Frontend Estático)                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐   │
│  │  index.html  │────▶│  login.html  │────▶│ authService  │   │
│  └──────────────┘     └──────────────┘     └──────┬───────┘   │
│         │                                           │            │
│         │                                           │            │
│         └───────────────────┬───────────────────────┘            │
│                             ▼                                    │
└─────────────────────────────┼────────────────────────────────────┘
                              │
                              │ HTTPS
                              │
┌─────────────────────────────▼────────────────────────────────────┐
│                      SUPABASE CLOUD                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                    SUPABASE AUTH                          │ │
│  │  • Email/Password Provider                                │ │
│  │  • Gestión de Sesiones (JWT)                             │ │
│  │  • Tokens de Acceso                                       │ │
│  │  • Recuperación de Contraseña                            │ │
│  └───────────────────────────┬───────────────────────────────┘ │
│                               │                                  │
│  ┌───────────────────────────▼───────────────────────────────┐ │
│  │                   POSTGRESQL DATABASE                     │ │
│  │                                                            │ │
│  │  ┌──────────────┐           ┌──────────────┐            │ │
│  │  │ auth.users   │           │   mensajes   │            │ │
│  │  ├──────────────┤           ├──────────────┤            │ │
│  │  │ id (uuid)    │◀──────────│ user_id (FK) │            │ │
│  │  │ email        │           │ categoria    │            │ │
│  │  │ password     │           │ texto        │            │ │
│  │  │ created_at   │           │ ...          │            │ │
│  │  └──────────────┘           └──────────────┘            │ │
│  │                                                            │ │
│  │  ┌──────────────┐           ┌──────────────┐            │ │
│  │  │ auth.users   │           │  reacciones  │            │ │
│  │  ├──────────────┤           ├──────────────┤            │ │
│  │  │ id (uuid)    │◀──────────│ user_id (FK) │            │ │
│  │  └──────────────┘           │ emoji        │            │ │
│  │                              │ mensaje_id   │            │ │
│  │                              └──────────────┘            │ │
│  │                                                            │ │
│  │  ┌────────────────────────────────────────┐              │ │
│  │  │       ROW LEVEL SECURITY (RLS)        │              │ │
│  │  │  • Lectura: Todos (público)           │              │ │
│  │  │  • Escritura: Solo autenticados       │              │ │
│  │  │  • Update/Delete: Solo el dueño       │              │ │
│  │  └────────────────────────────────────────┘              │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Flujo de Registro de Usuario

```
┌─────────┐
│ USUARIO │
└────┬────┘
     │
     │ 1. Visita login.html
     ▼
┌──────────────────────┐
│   Login Page (UI)    │
│  ┌────────────────┐  │
│  │ Tab: REGISTRO  │◀─── Selecciona "Registrarse"
│  └────────────────┘  │
└──────────┬───────────┘
           │
           │ 2. Completa formulario:
           │    • Email: leopal123@gmail.com
           │    • Password: ********
           │    • Confirmar: ********
           ▼
┌──────────────────────┐
│   authService.js     │
│                      │
│  signUp(email, pwd)  │◀─── 3. Valida datos
└──────────┬───────────┘
           │
           │ 4. API Request POST
           │    /auth/v1/signup
           ▼
┌──────────────────────┐
│   SUPABASE AUTH      │
│                      │
│  • Valida email      │
│  • Hash password     │
│  • Crea registro     │
│  • Genera tokens     │
└──────────┬───────────┘
           │
           │ 5. INSERT INTO auth.users
           ▼
┌──────────────────────┐
│   PostgreSQL DB      │
│                      │
│  auth.users:         │
│  ├─ id: uuid-123     │
│  ├─ email: leopal... │
│  └─ encrypted_pwd    │
└──────────┬───────────┘
           │
           │ 6. Return success + session
           ▼
┌──────────────────────┐
│   authService.js     │
│                      │
│  • Guarda sesión     │
│  • Actualiza UI      │
└──────────┬───────────┘
           │
           │ 7. Redirect
           ▼
┌──────────────────────┐
│    index.html        │
│  ┌────────────────┐  │
│  │ 👤 Logueado    │  │
│  └────────────────┘  │
└──────────────────────┘
```

---

## 🔓 Flujo de Inicio de Sesión (Login)

```
┌─────────┐
│ USUARIO │
└────┬────┘
     │
     │ 1. Clic "Iniciar Sesión"
     ▼
┌──────────────────────┐
│   Login Page (UI)    │
│  ┌────────────────┐  │
│  │  Tab: LOGIN    │◀─── Selecciona "Iniciar Sesión"
│  └────────────────┘  │
└──────────┬───────────┘
           │
           │ 2. Ingresa credenciales:
           │    • Email: leopal123@gmail.com
           │    • Password: ********
           ▼
┌──────────────────────┐
│   authService.js     │
│                      │
│ signIn(email, pwd)   │◀─── 3. Valida formato
└──────────┬───────────┘
           │
           │ 4. API Request POST
           │    /auth/v1/token?grant_type=password
           ▼
┌──────────────────────┐
│   SUPABASE AUTH      │
│                      │
│  • Busca usuario     │
│  • Verifica password │
│  • Genera JWT        │
└──────────┬───────────┘
           │
           │ 5. Return tokens
           │    {
           │      access_token: "eyJ...",
           │      refresh_token: "xxx",
           │      user: {...}
           │    }
           ▼
┌──────────────────────┐
│   authService.js     │
│                      │
│  • Guarda en memoria │
│  • Notifica cambio   │
└──────────┬───────────┘
           │
           │ 6. Update UI
           ▼
┌──────────────────────┐
│    index.html        │
│  ┌────────────────┐  │
│  │ 👤 leopal123...│  │ Botón cambia a verde
│  └────────────────┘  │
│                      │
│  Funcionalidades:    │
│  ✅ Crear mensajes   │
│  ✅ Reaccionar       │
└──────────────────────┘
```

---

## ✍️ Flujo de Creación de Mensaje (Protegido)

```
┌─────────┐
│ USUARIO │ (debe estar LOGUEADO)
└────┬────┘
     │
     │ 1. Clic "Agregar Mensaje"
     ▼
┌──────────────────────────┐
│  mensajesController.js   │
│                          │
│  crearBotonFormulario()  │
└──────────┬───────────────┘
           │
           │ 2. Abre modal
           ▼
┌──────────────────────────┐
│   Formulario Modal       │
│  ┌────────────────────┐  │
│  │ Categoría: [amor]  │  │
│  │ Texto: [Te amo...] │  │
│  │ Nota: [...]        │  │
│  │ [Guardar]          │  │
│  └────────────────────┘  │
└──────────┬───────────────┘
           │
           │ 3. Submit form
           ▼
┌──────────────────────────┐
│  mensajesController.js   │
│                          │
│  guardarMensaje(e)       │
└──────────┬───────────────┘
           │
           │ 4. ⚠️ VERIFICACIÓN DE AUTH
           │    if (!authService.isAuthenticated())
           │       ❌ Redirect a login.html
           │
           │ ✅ Está autenticado
           │
           │ 5. Prepara datos:
           │    mensaje = {
           │      categoria, emoji, texto,
           │      user_id: currentUser.id  ← IMPORTANTE
           │    }
           ▼
┌──────────────────────────┐
│   SUPABASE CLIENT        │
│                          │
│  .from('mensajes')       │
│  .insert([mensaje])      │
└──────────┬───────────────┘
           │
           │ 6. Request con JWT
           │    Authorization: Bearer eyJ...
           ▼
┌──────────────────────────┐
│   SUPABASE AUTH          │
│                          │
│  • Valida JWT            │
│  • Extrae user_id        │
└──────────┬───────────────┘
           │
           │ 7. RLS Check:
           │    auth.uid() = mensaje.user_id?
           │    ✅ Match → Permitir
           │    ❌ No match → Denegar
           ▼
┌──────────────────────────┐
│   PostgreSQL DB          │
│                          │
│  INSERT INTO mensajes    │
│  VALUES (...)            │
└──────────┬───────────────┘
           │
           │ 8. Return success
           ▼
┌──────────────────────────┐
│  mensajesController.js   │
│                          │
│  • Muestra notificación  │
│  • Limpia formulario     │
│  • Recarga lista         │
└──────────────────────────┘
```

---

## ❤️ Flujo de Reacción (Protegido)

```
┌─────────┐
│ USUARIO │ (debe estar LOGUEADO)
└────┬────┘
     │
     │ 1. Clic en emoji ❤️
     ▼
┌──────────────────────────┐
│   reaccionesModel.js     │
│                          │
│ insertarOActualizar...() │
└──────────┬───────────────┘
           │
           │ 2. ⚠️ VERIFICACIÓN DE AUTH
           │    if (!authService.isAuthenticated())
           │       ❌ Alert + Redirect a login.html
           │
           │ ✅ Está autenticado
           │
           │ 3. Obtiene user_id:
           │    sessionId = authService
           │                .getCurrentUser().id
           ▼
┌──────────────────────────┐
│   SUPABASE CLIENT        │
│                          │
│  • Busca reacción exist. │
│  • Si existe: toggle     │
│  • Si no: crear nueva    │
└──────────┬───────────────┘
           │
           │ 4. Request con JWT
           │    Authorization: Bearer eyJ...
           ▼
┌──────────────────────────┐
│   SUPABASE AUTH          │
│                          │
│  • Valida JWT            │
│  • Extrae user_id        │
└──────────┬───────────────┘
           │
           │ 5. RLS Check:
           │    auth.uid() = reaccion.user_id?
           │    ✅ Match → Permitir
           │    ❌ No match → Denegar
           ▼
┌──────────────────────────┐
│   PostgreSQL DB          │
│                          │
│  INSERT/UPDATE reacciones│
│  WHERE user_id = ...     │
└──────────┬───────────────┘
           │
           │ 6. Return success
           ▼
┌──────────────────────────┐
│   UI Update              │
│  ┌────────────────────┐  │
│  │ ❤️ 1               │  │ Contador actualizado
│  └────────────────────┘  │
└──────────────────────────┘
```

---

## 🔒 Row Level Security (RLS) - Funcionamiento

```
┌──────────────────────────────────────────────────────────────┐
│                    PETICIÓN DESDE FRONTEND                    │
│                                                               │
│  const { data, error } = await supabaseClient               │
│    .from('mensajes')                                          │
│    .insert([{ texto: "Te amo", user_id: "uuid-123" }])      │
│                                                               │
│  Header: Authorization: Bearer eyJhbGc...                    │
└──────────────────────────┬───────────────────────────────────┘
                           │
                           │ 1. JWT enviado
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                      SUPABASE AUTH                            │
│                                                               │
│  • Valida JWT signature                                      │
│  • Verifica expiración                                       │
│  • Extrae información:                                       │
│    └─ user_id: "uuid-123"                                   │
│    └─ role: "authenticated"                                 │
└──────────────────────────┬───────────────────────────────────┘
                           │
                           │ 2. Request pasa a PostgreSQL
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                      POSTGRESQL + RLS                         │
│                                                               │
│  Política activa:                                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ CREATE POLICY "Usuarios autenticados pueden crear"     │ │
│  │ ON mensajes FOR INSERT                                 │ │
│  │ TO authenticated                                       │ │
│  │ WITH CHECK (auth.uid() = user_id);                     │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                               │
│  Evaluación:                                                 │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ auth.uid() = "uuid-123"                                │ │
│  │ user_id = "uuid-123"                                   │ │
│  │                                                         │ │
│  │ ✅ "uuid-123" = "uuid-123"  → PERMITIR                 │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                               │
│  Si auth.uid() ≠ user_id → ❌ DENEGAR (403 Forbidden)       │
└──────────────────────────┬───────────────────────────────────┘
                           │
                           │ 3. Ejecuta INSERT
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                    TABLA: mensajes                            │
│  ┌──────────────────────────────────────────────────────┐   │
│  │ id    │ texto      │ user_id   │ created_at         │   │
│  ├───────┼────────────┼───────────┼────────────────────┤   │
│  │ 1     │ Te amo     │ uuid-123  │ 2025-01-24 ...     │   │
│  └──────────────────────────────────────────────────────┘   │
└──────────────────────────┬───────────────────────────────────┘
                           │
                           │ 4. Return success
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                      FRONTEND                                 │
│                                                               │
│  { data: { id: 1, texto: "Te amo", ... }, error: null }    │
└──────────────────────────────────────────────────────────────┘
```

---

## 🛡️ Escenario de Ataque Bloqueado

```
┌──────────────────────────────────────────────────────────────┐
│              ATACANTE intenta insertar con otro user_id       │
│                                                               │
│  const { data, error } = await supabaseClient               │
│    .from('mensajes')                                          │
│    .insert([{ 
│       texto: "Hack", 
│       user_id: "otro-uuid-456"  ← ⚠️ No es su ID
│    }])                                                        │
│                                                               │
│  Header: Authorization: Bearer eyJ... (user: uuid-123)       │
└──────────────────────────┬───────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                      RLS EVALUATION                           │
│                                                               │
│  WITH CHECK (auth.uid() = user_id);                          │
│                                                               │
│  auth.uid() = "uuid-123"        (del JWT)                    │
│  user_id = "otro-uuid-456"      (del payload)                │
│                                                               │
│  ❌ "uuid-123" ≠ "otro-uuid-456"  → DENEGAR                  │
└──────────────────────────┬───────────────────────────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────┐
│                      RESPUESTA                                │
│                                                               │
│  {                                                            │
│    data: null,                                               │
│    error: {                                                  │
│      message: "new row violates row-level security policy"  │
│      code: "42501"                                           │
│    }                                                          │
│  }                                                            │
└──────────────────────────────────────────────────────────────┘

RESULTADO: ⛔ ATAQUE BLOQUEADO POR RLS
```

---

## 📝 Estados del Usuario

```
┌──────────────────────────────────────────────────────────────┐
│                   ESTADOS DE AUTENTICACIÓN                    │
└──────────────────────────────────────────────────────────────┘

┌─────────────────┐
│ NO AUTENTICADO  │
├─────────────────┤
│ • user = null   │
│ • JWT = null    │
│                 │
│ Puede:          │
│ ✅ Ver mensajes │
│ ❌ Crear        │
│ ❌ Reaccionar   │
└────────┬────────┘
         │
         │ signIn() / signUp()
         ▼
┌─────────────────┐
│   AUTENTICADO   │
├─────────────────┤
│ • user = {...}  │
│ • JWT = "eyJ..."│
│                 │
│ Puede:          │
│ ✅ Ver mensajes │
│ ✅ Crear        │
│ ✅ Reaccionar   │
│ ✅ Editar suyos │
└────────┬────────┘
         │
         │ signOut()
         ▼
┌─────────────────┐
│ NO AUTENTICADO  │
└─────────────────┘
```

---

## 🎯 Puntos Clave de Seguridad

```
┌────────────────────────────────────────────────────────┐
│           MÚLTIPLES CAPAS DE SEGURIDAD                 │
├────────────────────────────────────────────────────────┤
│                                                         │
│  1️⃣ FRONTEND (authService.js)                         │
│     • Verifica si hay sesión                           │
│     • Bloquea UI si no autenticado                     │
│     • Redirige a login                                 │
│     ⚠️ Bypasseable (JavaScript client-side)           │
│                                                         │
│  2️⃣ JWT (Supabase Auth)                               │
│     • Token firmado criptográficamente                 │
│     • Expira en 1 hora                                 │
│     • No modificable sin invalidar firma               │
│     ✅ Seguro                                          │
│                                                         │
│  3️⃣ ROW LEVEL SECURITY (PostgreSQL)                   │
│     • Políticas a nivel de base de datos               │
│     • Imposible de bypassear desde frontend            │
│     • auth.uid() proviene del JWT verificado           │
│     ✅✅ MUY Seguro                                     │
│                                                         │
└────────────────────────────────────────────────────────┘

CONCLUSIÓN: Incluso si alguien modifica el JavaScript del 
frontend, RLS en PostgreSQL SIEMPRE protege los datos. 🛡️
```

---

**¿Listo para implementar?** 👉 Sigue [`GUIA-RAPIDA-AUTH.md`](./GUIA-RAPIDA-AUTH.md)
